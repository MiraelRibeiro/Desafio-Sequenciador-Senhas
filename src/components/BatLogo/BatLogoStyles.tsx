import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 25,
    color: '#E5BF3C',
  }, 
  image:{
    alignSelf: 'center',
    marginTop: 10,
    resizeMode: 'contain',
    height: 200,
  }
});